# RFB Radar App

A simple FastAPI backend that generates radar charts and insights.

## Endpoints

### POST /radar
Generate radar + insights.

### GET /radar-image
Retrieve the generated PNG radar chart.
